# Sample data

The offline player pool is generated deterministically by `src/data/loaders.py`.
All names begin with `Sample` so synthetic data cannot be mistaken for current
rankings or projections. Replace it by passing a normalized CSV to `load_players`.

